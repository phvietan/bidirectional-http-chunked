{
  "name": "bidirectional-http-chunked",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "server": "PORT=10000 bun run src/server.ts",
    "server:prod": "pm2 start 'bun run server' --name bidirectional-http-chunked",
    "client": "bun run src/client.ts"
  }
}
